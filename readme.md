##JS compiler completely written on JS

1. without any libraries/frameworks (only eslint)
2. only JS code (node compiler), without polifills
3. without browser global variables/context (window, document etc)
